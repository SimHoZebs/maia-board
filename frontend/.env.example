# Local dev only. The dev/preview server proxies these API paths to the LAN backend.
MAIA_API_TARGET=http://debian-server:8000
# Build output dir override (dist-browser for e2e, dist-profiling for perf).
# MAIA_BUILD_DIR=dist
